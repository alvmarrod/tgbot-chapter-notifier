name: rogerbot

services:
  roger:
    image: tgbot-chaptnotifier:2.0.0
    container_name: rogerbot
    restart: unless-stopped
    env_file:
      - ./.env
    volumes:
      - type: bind
        source: app/data
        target: /app/data
